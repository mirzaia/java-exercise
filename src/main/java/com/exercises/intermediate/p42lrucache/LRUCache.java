package com.exercises.intermediate.p42lrucache;

import java.util.LinkedHashMap;
import java.util.Map;

/**
 * Problem 42: LRU Cache (LeetCode 146, Medium)
 *
 * Design a Least Recently Used (LRU) cache with a fixed capacity. Support
 * get(key) (return the value or -1 if absent, and mark it recently used) and
 * put(key, value) (insert or update, evicting the least recently used entry
 * if the cache is over capacity). Both operations must run in O(1) average time.
 *
 * See README.md in this folder for the brief explanation and proposed solutions.
 */
public class LRUCache {
    public LRUCache(int capacity) {
        // TODO: Implement the solution
    }

    public int get(int key) {
        // TODO: Implement the solution
        return -1;
    }

    public void put(int key, int value) {
        // TODO: Implement the solution
    }
}
