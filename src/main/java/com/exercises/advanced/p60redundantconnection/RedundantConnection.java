package com.exercises.advanced.p60redundantconnection;

/**
 * Problem 60: Redundant Connection (LeetCode 684, Medium)
 *
 * A tree with n nodes had one extra edge added, creating exactly one cycle.
 * Given edges (added one at a time), return the edge that, if removed,
 * results in a tree with n nodes. If multiple answers exist, return the
 * one that occurs last in the input.
 *
 * Example: edges = [[1,2],[1,3],[2,3]] -> [2,3]
 * See README.md in this folder for the brief explanation and proposed solutions.
 */
public class RedundantConnection {
    public int[] findRedundantConnection(int[][] edges) {
        // TODO: Implement the solution
        return new int[0];
    }
}
