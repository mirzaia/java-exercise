package com.exercises.advanced.p54regularexpressionmatching;

/**
 * Problem 54: Regular Expression Matching (LeetCode 10, Hard)
 *
 * Given an input string s and a pattern p, implement regular expression
 * matching supporting '.' (matches any single character) and '*' (matches
 * zero or more of the preceding element). The match must cover the entire
 * input string.
 *
 * Example: s = "aa", p = "a*" -> true
 * See README.md in this folder for the brief explanation and proposed solutions.
 */
public class RegularExpressionMatching {
    public boolean isMatch(String s, String p) {
        // TODO: Implement the solution
        return false;
    }
}
